; PHP-FPM / CGI upload limits (read per directory by PHP-FPM, e.g. on
; Railway/nixpacks). Must be at least UploadController's limits (8 MB images,
; 15 MB audio) — PHP's default 2M/8M rejects most phone photos before Laravel
; even sees them. `php artisan serve` (CLI server) ignores this file: for
; local dev pass the flags instead, see CLAUDE.md.
upload_max_filesize = 20M
post_max_size = 25M
